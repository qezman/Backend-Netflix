name: Build and push docker image

on:
  push:
    branches: [main]

jobs:
  build-and-push-1:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout out the repository
        uses: actions/checkout@v2

      - name: Docker Build-1
        run: |
          hostnamectl

  build-and-push-2:
    runs-on: ubuntu-latest
    needs: build-and-push-1

    steps:
      - name: Checkout out the repository
        uses: actions/checkout@v2

  build-and-push-2-1:
    runs-on: ubuntu-latest
    needs: build-and-push-2

    steps:
      - name: Checkout out the repository
        run: hostnamectl

      - name: Docker Build-2
        run: |
          hostnamectl

  build-and-push-2-2:
    runs-on: ubuntu-latest
    needs: build-and-push-2

    steps:
      - name: Checkout out the repository
        uses: actions/checkout@v2

      - name: Docker Build-3
        run: |
          hostnamectl

# name: Build and push docker image

# on:
#   push:
#     branches: [main]

# jobs:
#   build-and-push-1:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout out the repo
#         uses: actions/checkout@v2

#       - name: Docker Build
#         run: |
#           docker build -t netflix_backend .

